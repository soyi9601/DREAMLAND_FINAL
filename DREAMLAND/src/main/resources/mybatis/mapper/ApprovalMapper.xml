<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.dreamland.prj.mapper.ApprovalMapper">

	 <select id="getApvNo"
	 				  resultType="int">
	 	SELECT ApvNo 
	 	  FROM APPROVAL
	 	 WHERE MAX(ApvNo)	 
	 </select>

   <insert id="insertAproval"
          parameterType="AppletterDto">
    INSERT APPROVAL (
      , APV_NO
      , EMP_NO
      , APV_WRITE_DATE
      , APV_KINDS
    ) VALUES (
        APV_SEQ.NEXTVAL
      , #{empNo}
      , CURRENT_DATE
      , #{appkinds}
    )
  </insert>
  
  <insert id="insertaApvWriter"
          parameterType="AppletterDto">
    INSERT APV_WRITER (
        APV_NO
      , WRITER_LIST
      , EMP_NO
    ) VALUES (
      , #{apvNo}
      , #{writerList}
      , #{empNo}
    )
  </insert>
  
  <insert id="insertApvRef"
          parameterType="AppletterDto">
    INSERT APV_REF (
        
      , REF_NO
      , EMP_NO
      , APV_NO
    ) VALUES (
      , 
      , 
      , 
    )
  </insert>
 
  <insert id="insertApvLetter"
          parameterType="AppletterDto">
    INSERT APV_LETTER (
        LETTER_NO
      , APV_NO
      , LETTER_TITLE
      , LETTER_DETAIL
    ) VALUES (
        LETTER_SEQ.NEXTVAL
      , #{apvNo}
      , #{title}
      , #{contents}
    )
  </insert>
  
  
  
 
  

  
  
</mapper>